This module ensures Unbuild Orders track all the components related to
a specific serial number and returns them when unbuilding. If some components
where modified in a repair, the unbuild takes into account them.
