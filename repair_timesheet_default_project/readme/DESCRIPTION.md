Adds a **Repair Timesheets Default Project** option under Settings \>
Repair, allowing a default Project and Task to be configured per company.
These are meant to pre-fill new timesheet lines added on a Repair Order,
so the Project and Task do not need to be picked manually each time.
